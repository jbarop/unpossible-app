# Database Configuration
POSTGRES_USER=unpossible
POSTGRES_PASSWORD=unpossible
POSTGRES_DB=unpossible
DATABASE_URL=postgresql://unpossible:unpossible@localhost:5432/unpossible

# Admin Authentication
# Generate a secure password for admin access
ADMIN_PASSWORD=change-me-to-a-secure-password

# Session Configuration
# Generate with: node -e "console.log(require('crypto').randomBytes(32).toString('hex'))"
SESSION_SECRET=change-me-to-a-random-32-byte-hex-string

# Logging
# Options: error, warn, info, debug
LOG_LEVEL=debug

# Server
PORT=3000
